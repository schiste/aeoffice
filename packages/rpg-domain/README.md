# RPG Domain

Tiny RPG/idle domain fixtures used to prove the shared game engine boundary.

This package owns RPG-specific semantic assets, one compiled map, demo entities,
and demo actions. It intentionally depends only on neutral engine packages and
does not import the virtual office domain.
