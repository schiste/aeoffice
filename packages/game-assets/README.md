# packages/game-assets

Domain-neutral asset metadata foundation.

This package defines the shared asset vocabulary used by product/domain
packages: sources, tilesets, visual tokens, atlas frames, footprints, anchors,
occlusion, interaction affordances, variants, manifest shape, and catalog
validation.

It must not contain office-specific, RPG-specific, account/platform-specific, or
product UI asset IDs.
