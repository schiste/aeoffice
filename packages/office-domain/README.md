# packages/office-domain

Virtual-office domain data and adapters.

This package owns office-specific assets, map presets, prompt-to-map behavior,
and presentation vocabulary. It can import neutral engine packages such as
`@aedventure/game-assets`, but neutral packages must not import this package.
