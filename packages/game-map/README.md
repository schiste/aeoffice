# packages/game-map

Domain-neutral semantic map compiler.

This package owns map definitions, tile layers, object placement, zones, spawn
points, blocked tiles, movement collision layers, and map validation. Product
domains provide catalogs, presets, prompts, labels, and domain-specific token
resolution.
