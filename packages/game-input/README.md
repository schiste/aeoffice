# packages/game-input

Domain-neutral browser input primitives for tile-based games.

This package owns keyboard-to-vector mapping, held movement state, run/walk
intent generation, d-pad and joystick primitives, interaction key mapping,
input telemetry, and sequence-numbered neutral input shapes. Product apps own
their UI buttons, shortcuts, labels, and any domain-specific action handling.
