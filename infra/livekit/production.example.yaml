# Production template for a later deployment pass.
# Do not use these placeholder keys as real secrets.

port: 7880
bind_addresses:
  - 0.0.0.0

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true

redis:
  address: valkey:6379

keys:
  replace_with_livekit_api_key: replace_with_livekit_api_secret
