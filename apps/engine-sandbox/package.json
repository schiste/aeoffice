{
  "name": "@aedventure/engine-sandbox",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b && npm run build:browser",
    "build:browser": "vite build --config vite.config.mjs",
    "dev:browser": "vite --config vite.config.mjs --host 127.0.0.1",
    "smoke": "npm run build:browser && node ../../scripts/engine-sandbox-smoke.test.cjs"
  },
  "dependencies": {
    "@aedventure/game-topology": "0.1.0",
    "@aedventure/game-world": "0.1.0"
  }
}
