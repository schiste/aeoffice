{
  "name": "@aedventure/web",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b && npm run build:browser",
    "build:browser": "vite build --config vite.config.mjs",
    "dev:browser": "vite --config vite.config.mjs --host 127.0.0.1"
  },
  "dependencies": {
    "@aedventure/protocol": "0.1.0",
    "@aedventure/shared-types": "0.1.0",
    "phaser": "^4.1.0"
  }
}
