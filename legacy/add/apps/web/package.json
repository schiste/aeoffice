{
  "name": "@add/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run wasm:build",
    "dev": "vite",
    "prebuild": "npm run wasm:build:release",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "precheck": "npm run wasm:build",
    "check": "tsc -b && node ../../scripts/check-data-ui.mjs src",
    "wasm:build": "../../scripts/wasm-build-web.sh",
    "wasm:build:release": "../../scripts/wasm-build-web.sh"
  },
  "dependencies": {
    "solid-js": "^1.9.11"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "typescript": "~5.9.3",
    "vite": "^7.1.0",
    "vite-plugin-solid": "^2.11.10"
  }
}
