# legacy

This directory contains imported upstream/reference code.

`skyoffice-original/` is preserved for provenance and selective reuse. It is
not the target architecture and should not receive product feature work.

